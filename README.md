# RESTler Report Parser

## How to use

Put the items in `bug_buckets` generated by RESTler parser directly into the `bug_buckets` folder in this project. Making the two folder idendical.

Then, our parser will read all the bugs and generate a human-readable HTML report.
